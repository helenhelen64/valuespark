{
  "name": "valuespark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist/assets && cp index.html styles.css app.js dist/ && cp -R assets/logo dist/assets/",
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "test": "node --test"
  }
}
